#!/bin/sh
branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"

if [ "$branch" = "main" ]; then
  echo "Direct commits on main are prohibited. Create a topic branch first." >&2
  echo "Example: git switch -c chore/describe-change" >&2
  exit 1
fi

exit 0

