# Tool-neutral skills mirror (canonical copy lives under .claude/skills/)
.agents/

# Local Claude Code project settings (not shared)
.claude/settings.json

# Codex agent definitions (kept local unless explicitly shared; .codex/skills/ remains tracked)
.codex/agents/*
!.codex/agents/surveyor-project-manager.toml
