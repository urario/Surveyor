name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Unit (headless)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET (from global.json)
        uses: actions/setup-dotnet@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-ubuntu-${{ hashFiles('Directory.Packages.props', 'global.json', '**/*.csproj') }}
          restore-keys: |
            nuget-ubuntu-

      - name: Restore
        run: dotnet restore eng/Surveyor.Unit.slnf

      # The unit lane runs in the default (Debug) configuration on purpose:
      # DES-0008/CS-07 define the gate as a plain `dotnet test`, and the
      # fresh-process probe tests spawn `dotnet test <project> --no-build`
      # without a configuration, so their child processes only find
      # binaries when the outer run uses the default configuration too.
      - name: Build
        run: dotnet build eng/Surveyor.Unit.slnf --no-restore

      # Coverlet enforces the CS-07 80% line-coverage gate on core test
      # projects via Directory.Build.props, so `dotnet test` fails the run
      # when coverage drops below threshold.
      - name: Test
        run: >
          dotnet test eng/Surveyor.Unit.slnf
          --no-build
          --logger trx
          --results-directory artifacts/test-results

      - name: Upload test and coverage results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-results
          path: |
            artifacts/test-results
            artifacts/coverage
          retention-days: 7
          if-no-files-found: ignore

  windows-build:
    name: Windows build + format
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET (from global.json)
        uses: actions/setup-dotnet@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-windows-${{ hashFiles('Directory.Packages.props', 'global.json', '**/*.csproj') }}
          restore-keys: |
            nuget-windows-

      # windows-latest is the only runner that can restore the
      # net10.0-windows TFM projects (App / Presentation / non-audit
      # adapters), so full-solution compile and format live here.
      - name: Build full solution
        run: dotnet build Surveyor.slnx -c Release

      - name: Test UIA adapter
        run: >
          dotnet test tests/Surveyor.Adapters.Uia.Tests/Surveyor.Adapters.Uia.Tests.csproj
          --no-build
          -c Release
          --logger trx
          --results-directory artifacts/test-results

      - name: Verify formatting (CS-09)
        run: dotnet format Surveyor.slnx --verify-no-changes --no-restore

      - name: Upload Windows test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-results
          path: artifacts/test-results
          retention-days: 7
          if-no-files-found: ignore

  knowledge:
    name: Knowledge base (OKF)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate OKF knowledge tree
        shell: pwsh
        run: ./tools/okf/Validate-Okf.ps1

      - name: Verify rq-index is up to date
        shell: pwsh
        run: |
          ./tools/requirements/Export-RqIndex.ps1
          git diff --exit-code -- knowledge/requirements/rq-index.generated.md
