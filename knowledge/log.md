# Surveyor Knowledge Log

## 2026-07-04

* **Design Alignment**: Updated `DES-0008` to name `Surveyor.slnx` as the canonical solution file for the new C#/.NET scaffold, aligning the detailed-design artifact with PR #85 implementation and minimizing drift between design and code.

* **Implementation Evidence**: Added `TRC-0002` for the `DES-0008` scaffold implementation slice. It records the `Surveyor.slnx` / `Surveyor.Unit.slnf` scaffold, ten `src` projects, `Surveyor.TestSupport`, `Surveyor.Architecture.Tests`, fixture/integration placement directories, architecture-test behavior names, the required forbidden-reference RED evidence (`Surveyor.Domain` -> `Surveyor.Application`), GREEN verification, and the carried Human validation risk for Windows-facing build / final TFM-SDK-Windows App SDK pins (`RQ-054`, `RQ-051`).

* **Review Response**: Tightened the `DES-0013` local-store temp-directory contract after PR #83 follow-up review. The atomic writer now requires `IAccessControlService.CreateUserOnlyDirectory` to create the temp run directory with a user-only security descriptor at creation time, verifies the temp ACL before writes, and fails with `IoError` if creation-time ACL hardening is unavailable. This closes the remaining mkdir-to-ACL race window for `RQ-052` / `R-SEC-02` and updates `UT-0009` expectations.

## 2026-07-03

* **Update**: Extended [Coding Standards](process/coding-standards.md) with quantitative, build-enforced quality gates (`CS-05`–`CS-10`) after owner approval of the tradeoffs: `CS-05` `AnalysisLevel=latest-All` (all Microsoft CA rules as build errors, with a justified `.editorconfig` suppression list and mandatory `Justification` on local suppressions); `CS-06` code-metrics thresholds enforced via `CA1501`/`CA1502`/`CA1505`/`CA1506` + `CodeMetricsConfig.txt` (cyclomatic complexity ≤ 10, inheritance depth ≤ 5, maintainability index ≥ 20, class coupling ≤ 30 — same metrics as the VS 2026 "Calculate Code Metrics" window, live in-editor); `CS-07` coverlet core-layer line-coverage hard gate ≥ 80% (Domain/Application/Policy/Reports; adapters/shell report-only); `CS-08` `PublicApiAnalyzers` public-surface tracking mechanizing the `CS-02` internal-default policy; `CS-09` `dotnet format --verify-no-changes` in the unit lane; `CS-10` periodic Stryker.NET mutation-score evidence (core target ≥ 80%, non-blocking, recorded under `knowledge/traces/`). `DES-0008` settings table updated accordingly (AnalysisLevel raised to `latest-All`, metrics/public-API/coverage rows added, unit lane runs format verification) with the §5.3 version note extended. Implementation and review skills/agents updated to run the gates before handoff and to treat unexplained suppressions and coverage padding as findings.

* **Creation**: Added [Coding Standards](process/coding-standards.md) (`knowledge/process/coding-standards.md`) fixing four implementation-phase decisions before the first code slice: `CS-01` Japanese XML documentation comments are mandatory on every public API in `src/**`, enforced mechanically via `GenerateDocumentationFile=true` + `CS1591` under the existing `TreatWarningsAsErrors` (tests/`Surveyor.TestSupport`/generated code excluded); `CS-02` internal-default + sealed-default accessibility with `public` reserved for assembly-boundary contracts and centrally granted `InternalsVisibleTo` for tests; `CS-03` SOLID mapped onto existing Surveyor structures (DES-0002 responsibilities, DES-0003 port contracts, DES-0008 mechanical dependency rule) instead of abstract slogans; `CS-04` a purpose-first GoF pattern vocabulary catalog (situation → recommended pattern → over-engineering signal) with a one-line pattern/purpose/tradeoff record required on application, consistent with the existing purpose-first review gate. `DES-0008`'s determinism/quality settings table gained the `GenerateDocumentationFile` and `InternalsVisibleTo` rows with a §5.3 version note (realization owned there; rules owned by the standard). Wired the standard into the agent assets so both AIs apply it consistently: `.codex/skills/surveyor-implementation` (workflow step + coding-standards section + handoff check), `.codex/skills/surveyor-detailed-design` (canonical input), `.claude/skills/surveyor-design-review` (SOLID / pattern-vocabulary / public-surface checklist items), `.claude/skills/surveyor-tdd-review` (InternalsVisibleTo + doc-comment quality checks), both `surveyor-quality-review` skills (required context), `.claude/agents/surveyor-architect` (gate items), `.claude/agents/surveyor-reviewer` (checks), plus `AGENTS.md` / `CLAUDE.md` pointers.

* **Review Response**: Closed the medium design-review findings from issues #32/#33/#35 for `DES-0010`, `DES-0011`, and `DES-0013`: defined the `ScoreResult.Confidence` derivation from participating axis confidence plus `unknownWeightBp` caps (`RQ-051`), fixed `PriorityBasis` shape and `ScreenSelectionMetadata` -> `PriorityBasis` mapping/timing without computed priority (`RD-016`), and made local-store atomic write apply user-only ACL to the temp run directory before final move (`RQ-052` / `R-SEC-02`). Added matching UT handoff expectations for `UT-0002`, `UT-0012`, and `UT-0009`.

* **Creation**: Added the [AI Design Review Strategy](process/ai-design-review-strategy.md) and the [Design Review Pattern Catalog](process/design-review-patterns.md) from the PR #81 review-rally retrospective (4 review rounds on `DES-0010`/`DES-0011`/`DES-0013`). The strategy moves each defect class to the cheapest layer that catches it: L0 — the DES-0007 §6 template now requires a **Contract closure** section (port-method I/O derivation, DTO field ownership, round-trip symmetry); L1 — the design author sweeps the ten seeded `DRP-xxx` patterns before the PR and records Self-Review Evidence in the PR body (new `.codex/skills/surveyor-detailed-design` authoring skill; `surveyor-design-review` self-check mode); L2 — the four hand-typed role prompts are replaced by a single review trigger delegating to the codified four-lens protocol in `.claude/skills/surveyor-design-review`, with findings tagged `DRP-xxx` + severity and a scoped verdict; L3 — review fixes are classified local vs boundary-reshaping, and boundary-reshaping fixes re-run the closure patterns `DRP-02`–`DRP-05` on the reshaped boundary with a contract-diff reply before re-review. New Critical/High findings that match no pattern are generalized into the catalog by `surveyor-knowledge-curator`. Metric: review rounds per design PR, baseline 4 (PR #81), target ≤ 1. `DES-0007` §5/§6/§9 updated with a §5.3 revision note; human final approval on every gate is unchanged.

* **Review Response**: Addressed the PR #81 review on commit `10c9125` about `ConfidentialityDecision` population and consistency. `DES-0011` now states that `IConfidentialityPolicy.Apply` owns the population of `SanitizedRunResult.ConfidentialityDecision`, `AnalyzeScreenUseCase` verifies decision equality before returning/saving, and `GenerateReportUseCase` copies the stamped decision into `ReportRequest` without re-deciding policy. `DES-0013` now defines the decision consistency contract across `PolicyApplicationResult`, `ProtectedRunModel`, `StoredResultDocument`, and `StoredRunSnapshot`, including load-time `SchemaInvalid` handling for missing or mismatched decision metadata.

* **Review Response**: Addressed the second follow-up PR #81 review on commit `2f56f5b` by resolving the save/load and report-generation asymmetry rather than adding another local patch. `DES-0011` now treats `PolicyApplicationResult.SanitizedRunResult` as the only UI/report input, removes `GenerateReportUseCase`'s policy dependency, defines `ReportRequest`, and changes `StoredRunResult` to return a `StoredRunSnapshot` rather than a bare `AnalysisRunResult`. `DES-0013` now defines the protected store serialization contract (`StoredResultDocument`, `StoredCaptureDocument`, `StoredReportDocument`, `MaskingDictionaryDocument`), the load reconstruction algorithm, and store/load symmetry tests so `ExportResultUseCase` can build a shareable export from a well-defined snapshot without treating encrypted blobs as report input.

* **Review Response**: Addressed the follow-up PR #81 review on commit `950d02d`, with the export/report load-shape later superseded by the snapshot refinement above. The initial response added `IResultStorePort.LoadRunAsync(RunId)`, clarified the report confidentiality sequence, and noted that confidentiality/export DTO field definitions are canonical in `DES-0013`. `DES-0010` routes non-fixable blocking findings to `NotEnoughEvidence` and restricts `LimitedAutomation` to non-blocking cases, removing the unreachable fallback row. `DES-0013` aligns `ILocalRunStore.LoadAsync` with the application-owned load port and documents load failure semantics.

* **Review Response**: Addressed PR #81's four-perspective design review findings for `DES-0010`/`DES-0011`/`DES-0013`. `DES-0010` now fixes integer-only signal weighting, aggregate rounding, ordered `TestabilityClass` classification, `FindingCode`/`RootCauseCode`/`CandidateCode` enums, config validation constraints, and additional `UT-0002` boundary/determinism cases. `DES-0011` now aligns with the basic-design use-case split (`SelectTargetUseCase`, `AnalyzeScreenUseCase`, `GenerateReportUseCase`, `ExportResultUseCase`), resolves `ScoringConfigReference` through `IScoringConfigProvider`, makes `IConfidentialityPolicy` an application-owned port with `Decide`/`Apply`/export-model creation, adds `IStageTimeoutController`, stage criticality, cancellation-vs-timeout race rules, and expanded `UT-0012` cases. `DES-0013` now makes `IConfidentialityPolicy` implementation-side only in `Surveyor.Policy`, defines previously missing policy/store/export DTO fields, clarifies `IResultStorePort` vs internal `IExportBundleWriter`, fixes export-local fallback pseudonyms, removes OCR/proof assumptions for unknown-bounds screenshots, adds DPAPI purpose strings, atomic export write/no-overwrite rules, corrupt-manifest retention behavior, file-system failure seams, and Windows integration/manual test boundaries.

* **Update**: Strengthened the `DES-0010`/`DES-0011`/`DES-0013` detailed-design batch with class-design evidence needed by downstream unit-test design. Each artifact now has a dedicated `Class Design (UML)` section and a `Public API Definitions` section with C# signatures, public DTO/interface/enum definitions, throw/status rules, and unit-test seam guidance: `DES-0010` fixes the `TestabilityScorer`/scoring record API for `UT-0002`; `DES-0011` fixes the use-case and port interfaces for fake-based `UT-0012`; `DES-0013` fixes confidentiality policy, sanitizer, fallback-key mapper, store/export, DPAPI, and ACL seams for `UT-0008`/`UT-0009`. A consistency self-review found the three documents now share the same detailed-design structure and handoff depth: trace block, module coverage, class UML, public APIs, behavior rules, edge cases, test intent, and implementation handoff.

* **Creation**: Added the detailed-design batch for issues #21/#22/#24: `DES-0010` scoring/classification/improvement candidates, `DES-0011` port DTOs/status/use-case orchestration, and `DES-0013` confidentiality/storage/export. The artifacts are written for direct implementation and unit-test design handoff: `DES-0010` fixes axis-to-UIA/MSAA signal mapping, versioned scoring config, basis-point rounding, class thresholds, unavailable-vs-low-score semantics, de-duplicated root causes, and no-priority candidate generation for `IMP-0002`/`UT-0002`; `DES-0011` fixes DTOs, status enums, timeout/cancellation, partial-result aggregation, safe diagnostics, ROI handoff, unchanged `ScreenSelectionMetadata` carriage, and `IClock` usage for `UT-0012`/use-case wiring; `DES-0013` fixes secure-by-default masking/redaction, fallback-key export pseudonyms, DPAPI `CurrentUser` local storage under `%LOCALAPPDATA%`, user ACLs, retention, sanitized diagnostics/exceptions, and policy-gated masked export for `UT-0008`/`UT-0009`/`IT-0004`.

* **Decision**: Promoted `ADR-0002` to **Accepted** after human approval on issue #30 (2026-07-03). The selected adapter technology remains raw COM UIA client, Windows.Graphics.Capture primary + `PrintWindow(PW_RENDERFULLCONTENT)` fallback, and unpackaged self-contained primary distribution; downstream residual risks are carried to `DES-0014`/`DES-0015`/`DES-0016`/`IT-0005` rather than leaving the adapter package gate closed.

* **Update**: Integrated the ADR-0002 spike measurements and raised the recommendation (issue #30). Added `TRC-0001` (`knowledge/traces/trc-0001-adr-0002-spike-measurements.md`) recording per-axis evidence from three target classes: the human owner's run against a real C++/MFC target (TortoiseGit — both UIA candidates deterministic across fresh processes, 48 elements, raw COM ~125 ms vs FlaUI ~156 ms; WGC captured compositor-side at physical DPI 664×796 while the non-PMv2 PrintWindow process got virtualized 455×537), a large Chromium tree (VS Code, 1029 elements — first-touch **lazy accessibility-tree realization** returned a 19-element skeleton, byte-stable post-realization; → `DES-0014` warm-up + `Unavailable(NotRealized)` rule), and WGC failure-mode probes (shell/UWP-frame windows → `CreateForWindow` `ArgumentException`; → `DES-0015` failure-mode table). `ADR-0002` updated from pending-measurements to **Proposed with recommendation**: raw COM UIA client (thin owned wrapper; 1:1 auditable RD-032 prohibited-pattern mapping, direct HRESULTs, ~15–20% faster), WGC primary + PrintWindow fallback, unpackaged-primary packaging; DES-0007 §4.2 exit criteria met with carried risks named (elevated-target calibration, mixed-DPI, `uiAccess`/MSIX, WGC border UX → `DES-0014`/`IT-0005`/`DES-0016`). Threading feeds to `DES-0011` recorded (MTA-safe, WGC ~400 ms first-frame budget, cancellation unexercised). **Final selection approval and promotion to Accepted remain the human owner's decision** (DES-0007 §8.1).

## 2026-07-02

* **Creation**: Added the `ADR-0002` adapter-technology spike scaffold (issue #30, `RSK-RD-001`, DES-0007 §4.2 hybrid owner — AI scaffolds, human runs, AI synthesizes): `spikes/adr-0002/` with four buildable PoCs (`UiaRawComPoc` raw COM via `Interop.UIAutomationClient`, `UiaFlaUiPoc` FlaUI.UIA3, `CapturePrintWindowPoc` GDI `PrintWindow(PW_RENDERFULLCONTENT)`, `CaptureWgcPoc` Windows.Graphics.Capture with D3D/WinRT interop), a shared `MeasurementReport` (per-axis JSON: elapsed, element count, canonical-tree SHA-256 with `Name` text hashed — never raw, `RQ-052` — unavailable counts, read-only API list), the `measurement/Measure-Spike.ps1` harness (runs UIA candidates twice in fresh processes for the determinism axis, aggregates a summary), a per-target `results-template.md` (pass/fail per DES-0007 §8 axis + IT-0001-style before/after read-only check + DES-0011/DES-0014/DES-0015 observation feeds), and `packaging/README.md` (unpackaged vs MSIX vs hybrid desk analysis + human verification steps). Added `knowledge/decisions/adr-0002-adapter-technology-selection.md` as **Proposed (draft)** with candidate desk analysis (leans: raw COM; WGC primary + PrintWindow fallback; unpackaged-primary) and pending measurement tables. Build green (0 warnings, .NET 10); smoke-run read-only on the dev machine — notable finding: WGC `CreateForWindow` fails for `Program Manager` (capture failure mode for `DES-0015`). Remaining for the human owner: run `Measure-Spike.ps1` against real legacy targets, fill the results template, packaging verification; then AI synthesizes `ADR-0002` and the human promotes it (gates #25/#26/#71/#72/#29).

* **Review**: AI pre-review of `DES-0009` completed (design gate #17 evidence, per DES-0007 §5.2): `surveyor-design-review` returned *Accept with changes* (Major: fallback NFC normalization conflicts with the `InvariantGlobalization=true` build setting; missing reconciliation note for the DES-0003/DES-0004 Stage-5 key-material wording) and `surveyor-quality-review` returned *Accept with risks* (screen identity-ladder terminal rule undefined; unqualified "label never changes key" claim vs rung-3 fallback; forgeable `#` collision suffix). All findings applied in the same revision: v=1 fallback normalization drops Unicode NFC (whitespace-collapse only, `char.IsWhiteSpace`); `ScreenIdentity` ladder + `n=0` no-identity terminal rule and source-tagged screen `id=` material; `#` added to `esc()` with a re-applied collision rule; exact window-class normalization regex table; label-change claims qualified to stable-identity rungs; counter-examples completed for all UT intent rows; `IFallbackKeyDerivation` homed in `Surveyor.Application.Ports` (implemented in `Surveyor.Policy`); honest provenance/enforcement wording for `FallbackKeyToken`; empty-screen Root rule; Stage-6 `SelectionMetadata` copy-with attachment; UT-0008 case homes split (Domain.Tests / Policy.Tests); explicit guardrail disposition for RQ-048/RQ-054. §5.3 version notes added to `DES-0003` (`IConfidentialityPolicy`) and `DES-0004` (Stage 5). New recorded residual risks: fallback-token guess-and-verify matching (exposure decision → `DES-0013`), rung-3 comparability degradation (`RD-021`), no-NFC v=1 limitation. Human owner final approval remains open on issue #17.

* **Creation**: Added `DES-0009` Domain Model, Stable Keys, and Availability detailed design (`knowledge/design/des-0009-domain-model-stable-keys-and-availability.md`), detailed-design package 2 ([DES-0007](design/des-0007-detailed-design-execution-strategy.md) §4, issue #16), covering `M04` and the `M11` `IClock` abstraction. Fixes the `ScreenModel`/`UiElement` value-object catalogue with invariants; the identity-source ladder (`AutomationId` → framework stable id → `M09` fallback hash → structural ordinal) with a canonical, versioned key-material encoding hashed by SHA-256 and compared ordinally (`R-NET-01`, never `Object.GetHashCode()`/iteration order), window-class normalization, and a deterministic sibling-collision rule; the `DisplayLabel`/key separation; and the **fallback-key minimal contract** (deterministic, non-reversible, cross-process stable, `IsFallback`-marked, domain never derives keys from raw sensitive text — `FallbackKeyToken` constructible only from `M09` output) **finalized at model construction (Stage 2)**, closing `RSK-DES-002`; later `DES-0013` policy changes follow the §5.3 supersede convention. Defines `Availability.Unavailable(reason)` (incl. `NotRealized` ≠ absent) as data distinct from low scores, orthogonal `AcquisitionConfidence`, screen/state-differentiated `ScreenKey`s (`RD-002`), the UTC-only `IClock` abstraction (concrete/threading → `DES-0011`/`DES-0018`), and `UT-0001`/`UT-0008` intent tables with counter-example fixtures (`R-QA-01`). Registered in `knowledge/index.md` / `knowledge/design/index.md`. Residual risk: implementation paths await the `DES-0008` scaffold; fallback *policy* breadth delegated to `DES-0013`; rung-1 runtime-generated-id detection carried to `DES-0014`. Pre-implementation review gate is issue #17.

* **Creation**: Added `tools/project/Sync-ProjectFields.ps1` to automate GitHub Project (`Surveyor Lifecycle Work` #1) field reflection. The script parses an Issue's "Project フィールド" block (and/or `-Set 'Field=Value'` overrides), auto-adds the item if needed, resolves single-select option ids from names, and writes `Status`/`Phase`/`Artifact`/`RQ`/`RD`/`Guardrail`/`Owner Role`/`Priority`/`Target` via `gh project item-edit` (requires the `project` token scope; `-DryRun` supported; ASCII-only source for PS 5.1). Documented the usage and the CI-full-automation caveat (a `project`-scoped PAT secret is needed because the default `GITHUB_TOKEN` cannot write user Projects v2) in `github-issue-project-workflow.md`. This retires the previous "manual Project reflection" limitation now that the token carries the `project` scope.

* **Creation**: Added `DES-0008` Project Structure and Test Harness detailed design (`knowledge/design/des-0008-project-structure-and-test-harness.md`), the first detailed-design package ([DES-0007](design/des-0007-detailed-design-execution-strategy.md) §4 package 1). Fixes the single `Surveyor.slnx` with ten `src` projects (`Surveyor.Domain`/`Application`/`Policy`/`Reports`/`Adapters.{Discovery,Uia,Capture,Store}`/`Presentation`/`App`) mapped to `M01`–`M13` ownership layers, root namespaces, and the inward dependency rule enforced mechanically by the `ProjectReference` graph plus a `Surveyor.Architecture.Tests` (`NetArchTest`/reflection) suite and `BannedApiAnalyzers` (ambient time/culture bans) — the failing-first mechanical evidence for `RQ-054`. Pins the `R-NET-02` determinism/quality settings centrally (`Directory.Build.props`: `net10.0`/`net10.0-windows` TFMs for VS 2026 18.7.3, `<Nullable>enable`, `<InvariantGlobalization>true`, `<Deterministic>true`, analyzers, warnings-as-errors; `global.json` SDK pin; `Directory.Packages.props` central versions). Places synthetic UT fixture trees/golden files under `tests/fixtures/`, defines the mixed IT fixture-app harness (`Surveyor.ITFixture.WinForms` first + skeleton real MFC/Win32 `Surveyor.ITFixture.Win32`, content deferred to `DES-0014`/`DES-0015`) per `R-OPS-03`, and the unattended unit lane vs documented-manual integration lane per `R-OPS-01`. Composition-root `M13` home located in `Surveyor.App` (wiring design stays `DES-0018`). Registered in `knowledge/index.md` and `knowledge/design/index.md`. Residual risk: incremental MFC fixture (skeleton first), packaging/signing provisional pending `ADR-0002`. Pre-implementation review gate is sub-issue #31.

* **Update**: Relaxed the Project Manager completion gate so `RQ` / `RD` are required only when present in the Issue or Project fields; `N/A` is allowed for Unit Test, Integration Test, and Review items without RD evidence to avoid invented trace IDs.

* **Creation**: Added Surveyor project management agent/skill assets for Codex and Claude Code. The new workflow manages GitHub Project fields, dependency/parallel planning, Owner Role assignment, risk tracking, and worker completion handoffs before Project status updates.

* **Update**: PR #14 のレビューコメント対応として、`Severity` が review-finding 専用 Issue Form フィールドであることを `github-issue-project-workflow.md` に明記し、`03-test-evidence.yml` に `Owner Role` / `Priority` / `Target` を追加して他の Issue Form と揃え、レビュー指摘対象ファイルの末尾改行を補正した。

## 2026-07-01

* **Creation**: GitHub Issue / Project の日本語運用ルールを `knowledge/process/github-issue-project-workflow.md` として追加。Issue 本文は日本語、Project フィールドは英語固定とし、`Status`/`Phase`/`Artifact`/`RQ`/`RD`/`Guardrail`/`Owner Role`/`Priority`/`Target`、4つの標準ビュー、親 Issue と sub-issues、Human / Claude Code / Codex の使い分け、Done 条件を定義した。

* **Decision**: Resolved carried human-decision item `R-SEC-02` (store at-rest protection + minimal privilege) — the local result store is **DPAPI (`CurrentUser`) encrypted at rest by default plus a user-restricted ACL under `%LOCALAPPDATA%`**, and export produces an explicit policy-gated masked shareable bundle (aligns with the secure-by-default ethos at low Windows cost, no manual key management; store is not portable across users/machines by design). The analyzer runs **same-integrity by default with no admin/`uiAccess` requirement**; elevation/`uiAccess` is an explicit opt-in (signed build) only when a target requires it — which also resolves the `uiAccess` signing/install aspect noted under `R-OPS-01`. Reflected in `DES-0007` §4 packages 6 (`DES-0013`) and 7 (`DES-0014`), §4.1, and the §8.1 resolved log; `DES-0005` adds a `UT-0009` at-rest obligation; review synced (`R-SEC-02` → ☑). With this, **all governance/process human-decision items from the review are resolved**; only the data-driven adapter technology pick (awaiting spike, human-approved via `ADR-0002`) remains carried.
* **Decision**: Resolved carried human-decision item `R-OPS-03` (IT fixture-app ownership) — **`DES-0008` owns the fixture-app harness (location/scaffold), `DES-0014`/`DES-0015` specify the legacy-edge and capture-failure content, built incrementally** (minimal early, expanded when the adapter packages land); technology is **mixed** — a small real MFC/Win32 app for authentic legacy edges (owner-draw, MSAA-only proxies, MDI, windowless, `WM_GETTEXT`) plus a lighter WinForms/synthetic surface for the general UIA cases, while UT keeps synthetic deterministic fixture trees. Reflected in `DES-0007` §4 package 1 scope and the §8.1 resolved log; review synced (`R-OPS-03` → ☑).
* **Decision**: Resolved carried human-decision item `R-OPS-01` (IT execution surface) — integration tests `IT-0001`–`IT-0007` run as a **documented manual gate on the developer's local Windows 11 machine for now** (no CI infra stood up while the source scaffold and fixture app do not yet exist); self-hosted interactive-runner automation of the automatable subset (e.g. read-only invariants `IT-0001`, capture `IT-0003`) is revisited once the fixture app and adapters exist. UT stays unattended/headless. Reflected in `DES-0007` §8.2 integration lane and the §8.1 resolved log; review synced (`R-OPS-01` → ☑).
* **Decision**: Resolved carried human-decision items `R-ARC-02`/`R-PM-01`/`R-AI-01` (spike process/ownership/schedule) — the `RSK-RD-001` adapter spike (UIA client raw COM vs FlaUI; capture PrintWindow vs Windows.Graphics.Capture) runs with a **hybrid owner** (AI scaffolds the candidate PoCs and per-axis measurement harness; the human owner runs them against real legacy targets and records live read-only/threading/DPI/permission behavior; AI synthesizes the draft `ADR-0002`) and is scheduled **early, in parallel with the core packages `DES-0009`–`DES-0012`**, feeding threading/async constraints into the `DES-0011` acquisition-port contracts before they are fixed. Only the data-driven final technology pick (human-approved, `ADR-0002` promotion) remains carried. Reflected in `DES-0007` §4.2 (spike *Owner*/*Timing*, DAG, critical-path note) and the §8.1 resolved log; review dashboard/Disposition Log synced (`R-ARC-02`/`R-PM-01` → ☑).
* **Decision**: Resolved carried human-decision items `R-PM-02`/`R-PM-03` — gate approval model is **AI reviewer agents pre-clear each gate (evidence via `surveyor-design-review`/`surveyor-quality-auditor`/`surveyor-reviewer`/`surveyor-tdd-review`) and the human owner gives final approval on every gate** (no gate closes on AI approval alone); the four §5.2 approver roles resolve to the single human owner on this small team. Detailed-design **phase completion requires an explicit human close sign-off** once the §8.2 criteria are met (no auto-close). Reflected in `DES-0007` §5.2, §8.2, and the §8.1 resolved-decisions log; review dashboard/Disposition Log synced to ☑.
* **Decision**: Resolved carried human-decision item `R-ARC-01` — the `M13` composition root / DI detailed design becomes a **standalone `DES-0018`** (execution order 11, finalized after the adapter spike since concrete provider wiring depends on the chosen adapter technology), rather than being folded into `DES-0008`. Rationale: RQ-054 (single concrete↔abstract seam) plus RQ-051/RQ-052 injection invariants (read-only-only adapters, single `IClock`, single `IConfidentialityPolicy`) warrant a dedicated reviewable home and a dedicated invariant test (`UT-0013`). Reflected in `DES-0007` §4 (package 11, DES-0008 no longer owns composition root), §4.1, §4.2 DAG, §8.1 resolved-decisions log, and trace-block ranges (now `DES-0008`–`DES-0018`, `UT-0001`–`UT-0013`); review dashboard/Disposition Log synced to ☑; `DES-0005` downstream range updated.
* **Update**: Integrated the `DES-0007 Multi-Perspective Expert Review` findings back into `DES-0007` (and `DES-0005`). Folded all Cowork-owned findings into the strategy: added a Modules (`M01`–`M13`) column with a coverage note closing the `M13` composition-root gap (R-ARC-01); enriched package scopes with the stable-hash/ordinal determinism rule (R-NET-01, Critical), project determinism settings (R-NET-02), axis↔UIA/MSAA property mapping (R-GTA-01), versioned externalized thresholds (R-MNT-01), serializer determinism + golden governance (R-NET-03/R-QA-02), log/diagnostics/exception sanitization (R-SEC-01), UIA threading + legacy acquisition edges + virtualization (R-WIN-02/03, R-GTA-02), PMv2 DPI awareness + capture failure modes (R-WIN-01/04), and diagnostics ownership (R-ARC-03); added §4.1 finding-traceable scope table, §4.2 dependency DAG + spike-as-ownable-task framing (R-ARC-02/R-PM-01/R-AI-01), §5.1 per-slice DoD (R-IMP-02), §5.2 approver roles (R-PM-02), §5.3 design supersede convention (R-MNT-02), §6 template rows (diagnostics, virtualization/capture edges, counter-example fixtures, minimal context bundle, module coverage), §7 determinism/counter-example/second-pass-smell rules (R-QA-01/R-AI-02), §8.1 carried human-decision risks, §8.2 CI/execution topology (R-OPS-01/02), §8 RSK-DES-002 front-loaded fallback-key contract (R-IMP-01), and §9 guardrail failing-first matrix + phase-completion criteria (R-QA-03/R-PM-03). Human-decision items (adapter selection/`ADR-0002`, IT runner, at-rest/ACL, approver owners, phase sign-off) are surfaced as explicit gates, not decided. Synced the review Summary Dashboard and added a Disposition Log (☑ 22 reflected / ◑ 9 Cowork-part-reflected-human-carried). Strengthened `DES-0005` UT/IT obligations (cross-process/culture determinism, legacy/virtualization fixtures, sanitization branch, new `UT-0013` composition-root invariants, mixed-DPI/capture-failure IT, counter-examples) without renumbering existing anchors.
* **Review**: Added `DES-0007 Multi-Perspective Expert Review` (`design/des-0007-review-multiperspective.md`) — an independent 11-perspective expert review of the `DES-0007` detailed-design execution strategy (architect, legacy Win32/MFC, C#/.NET, GUI test automation, QA, DevOps/CI, maintenance, implementer, PM, security/ops, Claude Code/Codex). 31 disposition-tracked findings (Critical 1 / High 12 / Medium 14 / Low 4), each typed as 事実/推測/提案 with severity, suggested tests, and 対応先. Top risks: `String.GetHashCode()` per-process randomization vs `RQ-051` (R-NET-01, Critical); missing composition-root/DI package (R-ARC-01); log/diagnostics/exception confidentiality egress outside the `RQ-052` gate (R-SEC-01); DES-0009↔DES-0013 fallback-key ordering conflict (R-IMP-01); IT execution environment/self-hosted Windows runner undefined (R-OPS-01); unscheduled/unowned adapter spikes (R-PM-01). The reviewed `DES-0007` file was not modified; the review links it as its upstream review target. (An initially-included medical-device safety perspective was removed at the requester's direction.)
* **Creation**: Added `DES-0007` detailed-design phase execution strategy under `knowledge/design/`. The strategy keeps detailed design inside OKF while making each future `DES-xxxx` self-contained, defines the planned package order (`DES-0008`-`DES-0017`), records why pure deterministic core design should lead Windows-adapter work, maps residual risks (`RSK-RD-001`-`RSK-RD-003`, `RSK-DES-001`, `RSK-DES-002`) to closure targets, and strengthens unit-test intent so `UT-0001`-`UT-0012` protect behavior rather than line coverage.
* **Review**: Independent re-review (`surveyor-quality-auditor`) of the corrected `DES-0002`–`DES-0005` returned **Accept** — all findings (`F-Major-1`, `F-Med-1`, `F-Min-1`, `F-Min-2`) verified closed with three-place consistency (per-module responsibility, DES-0005 trace row, named planned UT), the DES-0002 trace block confirmed bidirectionally honest (no `F-01`-style recurrence), full §5 basic-design scope `RD-002`–`RD-018` covered, and all four guardrails held (`RQ-052`/`RQ-051` mildly strengthened). Two minor detailed-design items were recorded as residual risk `RSK-DES-001` (assert analyzer records, never fabricates, user-supplied priority basis) and `RSK-DES-002` (pin fallback `ScreenKey` finalization stage) in `DES-0005`.
* **Update**: Applied independent basic-design review (`surveyor-quality-auditor`) corrections to `DES-0002`–`DES-0005`. Closed `F-Major-1` by assigning owners and downstream tests for previously unmapped in-scope requirements — `RD-002` (screen/state-differentiated evaluation unit → `M04`), `RD-015` (improvement-candidate generation → `M08`), `RD-016` (prioritization support via user-supplied `ScreenSelectionMetadata` → `M04`/`M03`/`M10`) — and corrected the `DES-0002` trace block from an overclaimed `RD-001`–`RD-032` range to the actually mapped set with explicit carve-outs for `RD-027`–`RD-029`/`RD-031`. Closed `F-Med-1` by splitting the collapsed evaluation trace into per-axis rows (`RD-007`/`RD-008`/`RD-009`/`RD-010` and `RQ-005`/`RQ-019`–`RQ-023`) with named `UT-0002` viewpoints. Addressed `F-Min-1` (M04 derives keys from non-sensitive stable identity; sensitive-fallback hashing delegated to `M09`) and `F-Min-2` (target-discovery candidate ordering scoped as within-session user-selection determinism, distinct from report/`ScreenKey` determinism). Guardrails `RQ-048`/`RQ-051`/`RQ-052`/`RQ-054` had passed the review unchanged.
* **Creation**: Added the basic design phase as `DES-0002`–`DES-0005` under `knowledge/design/`, taking `DES-0001` as input: `DES-0002` module responsibilities (13-module map, ownership layers, data ownership, guardrail assignment), `DES-0003` module interface contracts for every port and use case (direction, I/O, result/error model, cancellation, read-only/determinism/confidentiality constraints, fake strategy, open items, RQ/RD/UT/IT trace), `DES-0004` end-to-end analysis flow with run state machine, staged contracts, cancellation/partial-result rules, and guardrail checkpoints, and `DES-0005` V-model traceability mapping basic-design items to detailed design/implementation/UT/IT with planned `UT-0001`–`UT-0012` and `IT-0001`–`IT-0006` obligations and Codex slice candidates. Linked all four from `knowledge/index.md` and `knowledge/design/index.md`.
* **Creation**: Added `DES-0001` initial architecture design (Clean Architecture layers, MVVM boundaries, ports, determinism/read-only/confidentiality policies, technology allocation recommendation, and Codex slices) under `knowledge/architecture/`.
* **Update**: Applied third-party architecture review corrections to `DES-0001` — added `ITargetDiscoveryPort`/`SelectTargetUseCase` for the process/window boundary, separated `DisplayLabel` from key material with sanitization/hashing rules, made report/store operations cancellable with atomic writes, promoted read-only verification to a required adapter audit test plus an `IT-xxxx` invariant set, and aligned the trace block and per-slice `RQ`/`RD` with the driving requirements.
* **Enhancement**: Strengthened the `surveyor-architect` agent and `surveyor-design-review` skill for Clean Architecture, MVVM, interface-design, technology-allocation, and Mermaid-artifact review, with `RQ-048`/`RQ-051`/`RQ-052`/`RQ-054` as blocking guardrails, and added target-discovery-boundary, key/label separation, output/store cancellation, and read-only adapter-audit review items from the review.
* **Update**: Corrected requirement-definition review findings `F-01`, `F-03`, `F-04`, `F-05`, `F-08`, `O-01`, and `O-02` by aligning `RQ-002` traces, strengthening acceptance criteria, and linking OKF review summaries to the source document.
* **Creation**: Added OKF requirement-definition knowledge for the `RD-xxx` requirement definitions derived from the canonical `RQ-xxx` specification.
* **Creation**: Added lifecycle traceability policy for phase artifacts, stable artifact IDs, and required trace evidence blocks.
* **Creation**: Added `knowledge/design/` and `knowledge/traces/` as durable homes for design artifacts and trace evidence.
* **Creation**: Added Git policy for topic branches, pull requests, and no direct commits or pushes to `main`.
* **Creation**: Added Surveyor Git workflow skills and local Git hooks for branch guardrails.
* **Update**: Made RQ index refresh stable by preserving the existing generated timestamp and ignoring that timestamp in freshness checks.
* **Update**: Strengthened OKF validation for broken markdown links, index reachability, and generated RQ index freshness.
* **Update**: Changed generated RQ index output to UTF-8 without BOM and added OKF validation for BOM-free Markdown frontmatter previews.
* **Decision**: Added the common rule that uncertain requirements, architecture, process, Git workflow, and prior decisions start from `knowledge/index.md`.
* **Creation**: Added an ISO/IEC 25010-oriented quality review policy and independent quality review Agent/Skill assets.

## 2026-06-30

* **Initialization**: Created the OKF-style knowledge bundle for Surveyor.
* **Creation**: Added initial requirement source, architecture, process, and AI collaboration concepts.
* **Creation**: Added generated RQ index support through `tools/requirements/Export-RqIndex.ps1`.
* **Decision**: Established `.codex/skills/` as the canonical source for Codex skills; `~/.codex/skills` copies are optional personal installs.
* **Decision**: Clarified that OKF conformance applies to `knowledge/`; the requirement specification remains an ordinary canonical source document without OKF frontmatter.
* **Update**: Renamed the canonical requirements file to `docs/gui-testability-analyzer-requirements.md` for tool-friendly paths.
